# NatalTruth frontend — calculation API
# No trailing slash. Routes are /v1/... and /health (no /api prefix).
REACT_APP_BACKEND_URL=https://api.nataltruth.com

# Optional: OneSignal (unchanged from original frontend; not required for calc)
# REACT_APP_ONESIGNAL_APP_ID=
